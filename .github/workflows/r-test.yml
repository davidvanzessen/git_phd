name: Python package

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up R
        uses: conda-incubator/setup-miniconda@v2
        with:
          miniconda-version: "latest"
          mamba-version: "*"
          environment-file: requirements.yml
          activate-environment: git_phd
          auto-activate-base: false
      - name: run tests
        run: |
          Rscript pow.r
      